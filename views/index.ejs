<!DOCTYPE html>
<%- include ('partials/header.ejs') %>
<body>
    <%- include ('partials/navbar.ejs') %>
        <div class="container">
            <div class="card" id="form-question">
                <div class="card-header">
                    <h3>Jogos</h3>
                </div>
                <div class="card-body">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>
                                    Título
                                </th>
                                <th>
                                    Ano
                                </th>
                                <th>
                                    
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            <% games.forEach(game=> { %>
                                <tr>                                
                                    <td>
                                        <%= game.title %>
                                    </td>
                                    <td>
                                        <%= game.year %>
                                    </td>
                                    <td class="text-center">
                                        <a href="/jogo/deletar/<%= game.id %>" class="btn btn-danger">Deletar</a>
                                        <a href="/jogo/editar/<%= game.id %>" class="btn btn-primary">Editar</a>
                                    </td>                                
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
</body>
<%- include ('partials/footer.ejs') %>